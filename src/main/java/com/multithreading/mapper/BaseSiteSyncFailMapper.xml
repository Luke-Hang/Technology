<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multithreading.dao.BaseSiteSyncFailMapper">

    <insert id="save">
        insert into site5g.base_site_sync_fail(
            batch_no,
            district_code,
            district_name,
            base_site_code,
            base_site_name,
            raw_data_json,
            error_message,
            status
        )
        values (
            #{batchNo},
            #{districtCode},
            #{districtName},
            #{baseSiteCode},
            #{baseSiteName},
            #{rawDataJson},
            #{errorMessage},
            #{status}
        )
    </insert>
</mapper>
